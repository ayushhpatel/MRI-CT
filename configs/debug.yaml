data:
  mri_dir: "data/debug_mri"
  ct_dir: "data/debug_ct"
  num_workers: 0
model:
  n_res_blocks: 6
train:
  batch_size: 2
  epochs: 1
  lr: 0.0002
  lambda_cycle: 10.0
  lambda_identity: 5.0
  lsgan: true
  log_every: 5
  sample_every: 10
  save_every_steps: 20
  save_dir: "runs/debug"
  force_cpu: false